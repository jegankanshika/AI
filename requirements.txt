# pinned to versions verified during initial build (May 2026)
# agent runtime (Ollama is talked to over plain HTTP — no SDK needed)
langgraph>=1.2.0
fastapi>=0.136.0
uvicorn>=0.46.0
pydantic>=2.13.0

# document intelligence
pypdf>=6.0.0
reportlab>=4.5.0
cffi>=2.0.0

# data generation
faker==30.8.2
numpy==2.1.3
pandas==2.2.3

# audit persistence (use psycopg-binary alongside this for Postgres)
sqlalchemy>=2.0

# test
pytest>=9.0.0
python-multipart>=0.0.20
